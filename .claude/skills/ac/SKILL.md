---
name: ac
description: Shorthand alias for /ask-cellar — answers questions about Evan's wine collection. Use when the user types /ac.
---

# /ac

This is a short alias for `/ask-cellar`. It is not a separate skill — same
behavior, shorter to type.

Read `.claude/skills/ask-cellar/SKILL.md` in full and follow it exactly,
using whatever text the user passed after `/ac` as the question (same as
they would have passed after `/ask-cellar`).
